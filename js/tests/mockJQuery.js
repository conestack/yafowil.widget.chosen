// Import factory from jquery to allow access of global in chosen.jquery.js.
// This is required so that widget.js, test_chosen.js and chosen.jquery.js can
// use the same jQuery as global or via import.
import 'jquery-factory';
export default $;
